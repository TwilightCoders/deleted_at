require "spec_helper"

describe DeletedAt do
  it "has a version number" do
    expect(DeletedAt::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
